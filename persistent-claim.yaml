apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minecraft-server-data
spec:
  storageClassName: minecraft-server-data
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi